---
# Service account that pods use to assume the Bedrock IAM role
apiVersion: v1
kind: ServiceAccount
metadata:
  name: bedrock-sa
  namespace: tenant-b
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::__AWS_ACCOUNT_ID__:role/jabari-bedrock-inference-role
---
# Role: what operations are allowed in this namespace
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: tenant-b-role
  namespace: tenant-b
rules:
  - apiGroups: [""]
    resources: ["pods", "services", "endpoints", "configmaps"]
    verbs: ["get", "list", "watch"]
  - apiGroups: ["apps"]
    resources: ["deployments", "replicasets"]
    verbs: ["get", "list", "watch"]
---
# RoleBinding: assign the role to the service account
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: tenant-b-rolebinding
  namespace: tenant-b
subjects:
  - kind: ServiceAccount
    name: bedrock-sa
    namespace: tenant-b
roleRef:
  kind: Role
  apiGroup: rbac.authorization.k8s.io
  name: tenant-b-role
---
# ResourceQuota: cap resource consumption per tenant
apiVersion: v1
kind: ResourceQuota
metadata:
  name: tenant-b-quota
  namespace: tenant-b
spec:
  hard:
    requests.cpu: "4"
    requests.memory: 8Gi
    limits.cpu: "8"
    limits.memory: 16Gi
    pods: "20"
